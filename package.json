{
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0-beta.8",
    "tailwindcss": "^4.0.0-beta.8"
  },
  "scripts": {
    "build": "hugo --gc --minify --cleanDestinationDir",
    "dev": "hugo serve"
  }
}
